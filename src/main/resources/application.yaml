spring:
  application:
    name: logrove
  profiles:
    include: secret          # ← 추가

  datasource:
    url: jdbc:mysql://3.35.209.68:3306/logrove_test?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: logrove
    # password는 secret에서 읽음

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

jwt:
  expiration: 86400000

gemini:
  api:
    tag-url: https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent
    eval-url: https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent

storage:
  upload-dir: ./uploads