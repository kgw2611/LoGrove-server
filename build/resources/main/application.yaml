spring:
  application:
    name: logrove
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  security:
    user:
      name: admin
      password: 1234

gemini:
  api:
    key: ${GEMINI_API_KEY}